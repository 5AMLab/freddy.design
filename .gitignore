# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local agent / editor config
/.claude/

# local working assets — source files, references, raw exports (not shipped)
/Reference/
/Canela_Collection/
/public/portfolio/hero video/
# raw 3D render source PNGs (36–88MB each, ~404MB total) — only the derived
# /public/3drender/hero-object.webp is shipped
/public/3d render/
*.psd
# homepage redesign source files — raw PSDs/JPGs (~1.3GB), not referenced by
# any code; the derived assets that ARE used live under /public
/website design/

# scratch
/preview.html
